// Note for this lib to work, label is the global object the label from the model is stored in
let label = null;
let TML5_CONFIG = {
    starting_pos: {
        'x': 460,
        'y': 0
    },
    video_config: {
        'x': 500,
        'y': 384 
    },
    model_url: 'https://teachablemachine.withgoogle.com/models/U6_Rw9Ic/model.json'
};